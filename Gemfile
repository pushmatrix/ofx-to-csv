source :rubygems

gem 'ofx-parser', '0.1.0', :git => "git://github.com/pushmatrix/ofx-parser.git"
gem 'fastercsv'
gem 'hpricot'

# Manually require i18n because ActiveSupport, for whatever reason, doesn't require it
gem 'i18n'
gem 'activesupport'